# Web App

This app is a lightweight status surface for the repo while the runtime JSON
editor implementation is rebuilt.

## Current role

- Communicate the product direction clearly
- Avoid showcasing stale custom-language behavior
- Stay small until the JSON-mode editor integration is implemented
