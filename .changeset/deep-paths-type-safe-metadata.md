---
"@zod-monaco/core": minor
---

Add type-safe field metadata paths with `DeepPaths<T>` — full IDE autocomplete for `describeSchema` metadata fields up to 10 levels deep. Also adds optional nested object format with `_meta` sentinel as an alternative to flat dot-notation keys.
